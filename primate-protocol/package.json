{
  "name": "primate-protocol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint . --ignore-pattern .next --ignore-pattern out --ignore-pattern dist --ignore-pattern android",
    "test": "node --test",
    "convex:dev": "npx convex dev"
  },
  "dependencies": {
    "@babylonjs/core": "^8.29.0",
    "convex": "^1.30.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.35.0",
    "eslint": "^9.35.0",
    "globals": "^15.10.0",
    "vite": "^7.1.3"
  }
}
